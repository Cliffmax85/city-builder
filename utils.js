export function makeStatsString(skylineCount, castleCount, waterfrontCount) {
    return `You clicked on the skyline ${skylineCount} times, on the castle ${castleCount} times, and on the waterfront ${waterfrontCount} times.`;
}